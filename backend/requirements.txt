# SynapseGrid MVP 后端依赖
# 安装: pip install -r requirements.txt

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- 数据验证 ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- HTTP 客户端（用于 LLM API 调用）---
httpx>=0.27.0

# --- 模板 ---
PyYAML>=6.0

# --- Token 计数（可选）---
# tiktoken>=0.7.0

# --- Ed25519 签名（可选）---
# pynacl>=1.5.0

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
